* 重构消息处理，返回值使用 STOP 和 CONTINUE
* 禁言后避免因自动回复出来死循环
* 取消对 mongokit 的依赖
* 在群重启/关闭时的 presence 中附带消息
* 命令行参数（指定配置文件等）
* 支持 HTML 消息

* XMPP
  * 同步数据库和 roster 中的昵称
  * 删除订阅不为 'both' 的 roster item
* 统计
  * 流量统计
  * 命令使用统计
  * 记录最后一次发言时间
* 异常处理
  * 使用 -old 输出大量消息时，连接可能被重置。此时未及时发出（-old 命令之后）的消息不会被发出
